const size = {
    lt: '400px',
    xs: '460px',
    sm: '600px',
    bs: '800px',
    md: '1000px',
    bg: '1400px'
}

const breakpoints = {
    lt: `(max-width: ${size.lt})`,
    xs: `(max-width: ${size.xs})`,
    sm: `(max-width: ${size.sm})`,
    bs: `(max-width: ${size.bs})`,
    md: `(max-width: ${size.md})`,
    bg: `(max-width: ${size.bg})`,
}

export default breakpoints