import React from 'react';
import * as S from './ContainerCardsStyles';

const ContainerCards = () =>{
    return(
        <S.ContainerCards> </S.ContainerCards>
    );
};

export default ContainerCards